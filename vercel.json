{
  "version": 2,
  "builds": [
    {
      "src": "server/vercel-server.js",
      "use": "@vercel/node",
      "config": {
        "includeFiles": ["server/**", "src/**", "public/**", "build/**"]
      }
    }
  ],
  "routes": [
    {
      "src": "/api/(.*)",
      "dest": "/server/vercel-server.js"
    },
    {
      "src": "/(.*)",
      "dest": "/server/vercel-server.js"
    }
  ]
}